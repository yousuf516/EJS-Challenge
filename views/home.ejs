<%- include('partials/header'); -%>

<h1>Home</h1>

<p><%= homeSpeech %></p>

    <% posts.forEach(function(post){ %>
    <h1><%= post.postTitle %></h1><br>
    <%= post.postContent.substring(0,100) + "..." %> <a href="/posts/<%= post._id %>">Read more</a>
    <% }) %>

<%- include('partials/footer'); -%>